scheduling:
  enabled: false

spring:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/opensrp_test
    username: opensrp_admin
    password: admin
    driver-class-name: org.postgresql.Driver
  target-datasource:
    name: target
    jdbc-url: jdbc:postgresql://localhost:5432/dbconverter_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver