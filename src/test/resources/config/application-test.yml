scheduling:
  enabled: false

spring:
  datasource:
    jdbc-url: jdbc:postgresql://postgres:5432/opensrp_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  destination-datasource:
    name: destination
    jdbc-url: jdbc:postgresql://postgres:5432/dbconverter_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
